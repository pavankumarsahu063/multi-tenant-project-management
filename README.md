# Multi-Tenant Project Management Platform

A **full-stack SaaS Project Management Platform** designed for **multiple companies (tenants)** to manage projects, tasks, and teams independently. 

This platform is built using **Angular** (frontend), **Spring Boot** (backend), and **SQL** (database), following best practices in **system design, security, and scalability**.
